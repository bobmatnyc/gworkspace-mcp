id: "0001_rename_credentials_dir"
version: "0.2.0"
from_version: "0.1.x"
description: "Rename credentials directory from google-workspace-mcp to gworkspace-mcp (project-level)"
created_at: "2026-02-13"
operations:
  - type: move_directory
    from: "./.google-workspace-mcp"
    to: "./.gworkspace-mcp"
    backup: true
    skip_if_target_exists: true
